<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Your Chat!</title>
    <style>
      /* General Styles */
      body {
        background-color: #121212;
        color: #e0e0e0;
        font-family: "Arial", sans-serif;
        margin: 0;
        padding: 0;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
      }

      /* Container */
      .container {
        background-color: #1e1e1e;
        padding: 2rem;
        border-radius: 10px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
        max-width: 500px;
        width: 90%;
        text-align: center;
      }

      /* Heading */
      h1 {
        color: #f0a500;
        font-size: 1.8rem;
        margin-bottom: 1rem;
      }

      /* Paragraph */
      p {
        font-size: 1rem;
        margin-bottom: 1rem;
        color: #bbbbbb;
      }

      /* Textarea */
      textarea {
        width: 94%;
        height: 100px;
        padding: 0.5rem;
        border-radius: 6px;
        border: 1px solid #444;
        background-color: #2a2a2a;
        color: #e0e0e0;
        font-size: 1rem;
        resize: none;
        margin-bottom: 1rem;
      }

      textarea:focus {
        outline: none;
        border-color: #f0a500;
        box-shadow: 0 0 5px rgba(240, 165, 0, 0.8);
      }

      /* Button */
      button {
        font-size: 1rem;
        padding: 0.7rem 1.5rem;
        border: none;
        border-radius: 6px;
        background-color: #4caf50;
        color: white;
        cursor: pointer;
        transition: background-color 0.3s ease;
      }

      button:hover {
        background-color: #45a049;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>Edit Your Chat</h1>
      <p>
        This chat was sent from <strong><%= chat.from %></strong> to
        <strong><%= chat.to %></strong>.
      </p>
      <form method="POST" action="/chats/<%= chat.id %>?_method=PUT">
        <textarea name="msg" placeholder="Edit your message here...">
<%= chat.msg %></textarea
        >
        <br />
        <button type="submit">Save Changes</button>
      </form>
    </div>
  </body>
</html>
